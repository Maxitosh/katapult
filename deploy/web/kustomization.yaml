apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
configMapGenerator:
  - name: katapult-web-nginx
    files:
      - default.conf=nginx.conf
generatorOptions:
  disableNameSuffixHash: true
